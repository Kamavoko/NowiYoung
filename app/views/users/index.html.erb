<h1><%= @user.name %></h1>

    <table class='table table-striped table-bordered'>
      <tr>
        <th>あ</th>
        <th>い</th>
      </tr>

      <tr>
        <td>なまえ</td>
        <td><%= @user.name %></td>
      </tr>
      <tr>
        <td>アイコン</td>
        <td>
          <% if @user.image? %>
    　　  <%= image_tag @user.image.thumb.url %>
    　   <% else %>
    　　  <%= image_tag 'a.png' %>
    　   <% end %>
        </td>
      </tr>
      <tr>
        <td>メールアドレス</td>
        <td><%= @user.email %></td>
      </tr>
    </table>
<%= link_to "Home" , "/shops" %>
